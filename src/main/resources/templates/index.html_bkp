<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml"
      xmlns:th="http://www.thymeleaf.org"
      lang="en">
<head>
    <meta charset="UTF-8">
    <title>Taming Thymeleaf</title>
</head>
<body>
<h1 th:text="${pageTitle}">Taming Thymeleaf</h1>
<div>
    <ul>
        <li th:each="scientist : ${scientists}">
            <span th:text="${scientist}"></span>
        </li>
    </ul>

    <form action="#" th:action="@{/listMarvelUniverse}" th:object="${model}" method="post">
        <input type="text" th:field="*{name}" placeholder="Employee Name"
               class="form-control mb-4 col-4">
        <button type="submit">List Marvel Universe</button>
    </form>




</div>
</body>
</html>